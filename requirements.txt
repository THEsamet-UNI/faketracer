Flask>=2.0
werkzeug>=2.0
requests>=2.28
beautifulsoup4>=4.12
lxml>=4.9
opencv-python>=4.6.0
ffmpeg-python>=0.2.0
rq>=1.10.0
redis>=4.5.0
pillow>=9.0.0
pytesseract>=0.3.10
# Note: heavy optional ML packages (torch, easyocr, sentence-transformers)
# have been moved to `requirements-optional.txt` to keep Docker image small.
# Install them only when you need deep model inference on the same host.